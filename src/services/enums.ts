export enum Navigation {
  Home = '/',
  ReviewJob = '/review-job',
}

export enum SliceNames {
  job = 'job',
  ui = 'ui',
}

export enum JobStatus {
  completed = 'completed',
  pending = 'pending',
}

export enum LSKeys {
  warnings = 'warnings',
}

export enum IssuesTypes {
  issueFireResolution = 'issue-fire-resolution-event',
  issueAssigneeId = 'issue-assignee-id',
  issueTypeClass = 'issue-type-class',
}

export enum StorageKeys {
  editIssues = 'editIssues',
}
